$kind: http-request
url: '{{base_url}}/movies/{{id}}/vote'
method: POST
pathVariables:
  - key: id
    value: ''
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{access_token}}'
order: 2000000000000
